# Інструкція з встановлення EnergyBot на Windows

## Вимоги
- Windows 10/11
- Python 3.8 або новіше
- Git (опціонально)
- Доступ до локальної мережі з камерами

## Крок 1: Завантажте проект

### Варіант А: Через Git
```powershell
git clone https://github.com/VitaliiBazylo/energy_bot.git
cd energy_bot
```

### Варіант Б: Завантажити ZIP
1. Відкрийте https://github.com/VitaliiBazylo/energy_bot
2. Натисніть "Code" → "Download ZIP"
3. Розпакуйте в будь-яку папку (наприклад: `C:\EnergyBot`)

## Крок 2: Встановіть Python

1. Завантажте Python з https://www.python.org/downloads/
2. **ВАЖЛИВО:** Під час встановлення поставте галочку "Add Python to PATH"
3. Перевірте встановлення:
```powershell
python --version
```

## Крок 3: Налаштуйте проект

### Автоматично (рекомендовано)
Двічі клацніть на файл `setup.bat`

### Вручну
```powershell
# Перейдіть до папки проекту
cd C:\EnergyBot

# Створіть віртуальне оточення
python -m venv .venv

# Активуйте віртуальне оточення
.venv\Scripts\Activate.ps1

# Встановіть залежності
pip install -r requirements.txt
```

## Крок 4: Налаштуйте .env файл

1. Скопіюйте файл `.env.example` → `.env`
2. Відредагуйте `.env`:
```
BOT_TOKEN=ваш_токен_від_BotFather
CHAT_IDS=ваш_telegram_chat_id
```

### Як отримати токен бота:
1. Напишіть @BotFather в Telegram
2. Відправте `/newbot`
3. Введіть назву та username
4. Скопіюйте токен

### Як дізнатися chat_id:
```powershell
python get_chat_id.py
```
Відправте будь-яке повідомлення вашому боту

## Крок 5: Налаштуйте камери

Відредагуйте `cameras.json`:
```json
[
  {
    "name": "Hikvision Camera",
    "ip": "192.168.1.195"
  }
]
```

Замініть IP на адресу вашої камери.

## Крок 6: Запустіть бота

### Варіант А: Через start.bat (рекомендовано)
Двічі клацніть на файл `start.bat`

### Варіант Б: Вручну
```powershell
.venv\Scripts\python.exe main.py
```

## Крок 7: Автозапуск при завантаженні Windows

1. Натисніть `Win+R`
2. Введіть `shell:startup`
3. Скопіюйте ярлик `start.bat` в цю папку

## Перевірка роботи

Відправте боту команду `/status` в Telegram.

Має прийти відповідь:
```
📊 Статус:
🟢 ⚡ Світло Є
🕐 [час]
```

## Команди бота

- `/start` - Початок роботи
- `/status` - Поточний стан
- `/cameras` - Список камер
- `/help` - Допомога

## Підтримка

GitHub: https://github.com/VitaliiBazylo/energy_bot
