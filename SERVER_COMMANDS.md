# 🤖 EnergyBot - Команди управління на сервері

## 🚀 ЗАПУСК БОТА

### Простий запуск у фоні:
```bash
chmod +x start_bot.sh
./start_bot.sh
```

### Через скрипт управління:
```bash
chmod +x bot_control.sh
./bot_control.sh start
```

---

## 🔍 ПЕРЕВІРКА РОБОТИ

### Статус бота:
```bash
./bot_control.sh status
```

### Перевірка процесу:
```bash
ps aux | grep main.py
pgrep -f main.py
```

---

## 📋 ЛОГИ

### Перегляд логів в реальному часі:
```bash
./bot_control.sh logs
# або
tail -f bot.log
```

### Перегляд останніх 50 рядків:
```bash
tail -n 50 bot.log
```

### Очистка логів:
```bash
> bot.log
```

---

## 🛑 ЗУПИНКА БОТА

### Через скрипт:
```bash
./bot_control.sh stop
```

### Вручну:
```bash
pkill -f main.py
```

---

## 🔄 ПЕРЕЗАПУСК

### Через скрипт:
```bash
./bot_control.sh restart
```

### Вручну:
```bash
pkill -f main.py
./start_bot.sh
```

---

## 🔧 НАЛАШТУВАННЯ АВТОЗАПУСКУ

### Додати в crontab для автозапуску після перезавантаження:
```bash
crontab -e
```

### Додати цей рядок:
```
@reboot /повний/шлях/до/EnergyBot/autostart_bot.sh
```

### Додати автоматичний моніторинг (перевірка кожні 5 хвилин):
```
*/5 * * * * /повний/шлях/до/EnergyBot/monitor_bot.sh
```

### Перевірити crontab:
```bash
crontab -l
```

---

## 📁 СТРУКТУРА ФАЙЛІВ

```
EnergyBot/
├── main.py                 # Головний файл бота
├── config.py              # Конфігурація
├── monitor.py             # Модуль моніторингу
├── .env                   # Змінні середовища
├── cameras.json           # Список камер
├── requirements.txt       # Залежності
├── start_bot.sh          # Скрипт запуску
├── bot_control.sh        # Скрипт управління
├── monitor_bot.sh        # Скрипт моніторингу
├── autostart_bot.sh      # Скрипт автозапуску
├── bot.log              # Логи бота
├── bot.pid              # PID файл
└── .venv/               # Віртуальне середовище
```

---

## 🐍 PYTHON СЕРЕДОВИЩЕ

### Активація віртуального середовища:
```bash
source .venv/bin/activate
```

### Встановлення залежностей:
```bash
pip install -r requirements.txt
```

### Оновлення залежностей:
```bash
pip install --upgrade -r requirements.txt
```

---

## 🚨 ДІАГНОСТИКА ПРОБЛЕМ

### Перевірка портів:
```bash
netstat -tulpn | grep python
```

### Перевірка помилок в логах:
```bash
grep -i error bot.log
grep -i warning bot.log
```

### Тестування підключення до камери:
```bash
ping -c 5 192.168.1.195
```

### Перевірка змінних середовища:
```bash
cat .env
```

### Ручний запуск для дебагу:
```bash
source .venv/bin/activate
python main.py
# Бачите всі помилки в консолі
```

---

## 📊 МОНІТОРИНГ

### Використання ресурсів:
```bash
ps aux | grep main.py
top -p $(pgrep -f main.py)
```

### Простір на диску:
```bash
df -h
du -sh EnergyBot/
```

---

## 🔐 БЕЗПЕКА

### Права доступу до файлів:
```bash
chmod 600 .env
chmod +x *.sh
```

### Перевірка прав:
```bash
ls -la
```

---

## 💡 КОРИСНІ ПОРАДИ

1. **Завжди перевіряйте логи** після запуску
2. **Налаштуйте автоматичний моніторинг** через crontab
3. **Регулярно перевіряйте** стан бота
4. **Зберігайте резервні копії** .env та cameras.json
5. **Оновлюйте залежності** для безпеки

---

## 📞 ШВИДКА ДОВІДКА

```bash
# Запуск
./bot_control.sh start

# Статус
./bot_control.sh status

# Логи
./bot_control.sh logs

# Зупинка
./bot_control.sh stop

# Перезапуск
./bot_control.sh restart
```
